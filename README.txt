==================================================================
Working with GITHUB

cd ~/gitronen

git add .
git commit -m "ddddddddddddddddddddddd"
git push

user = ronentanami
pass = ronen10

In case push fails: git push --all --force